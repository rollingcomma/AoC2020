const { readInput, part1, part2 } = require('./src/day13/functions');

const inputs = readInput("input.txt");
//const result = part1(inputs);
const result = part2(inputs["busSchedules"]);

console.log(result)
