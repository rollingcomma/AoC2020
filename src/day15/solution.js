const { readInput, part1, part2 } = require('./functions');

const inputs = readInput("input.txt")[0];
//const result = part1(inputs,2020);
const result = part1(inputs, 30000000);

console.log(result)
